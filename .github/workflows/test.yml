name: Test

on:
  pull_request:
  push:
    branches:
      - '*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - env:
          ACTIONS_STEP_DEBUG: true
        run: |
          echo "::debug::a"
          echo "::notice::b"
          echo "::warning::c"
          echo "::error::d"
          echo "::debug::a\nb"

